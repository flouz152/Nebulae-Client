Place the original LabyMod 3 and mapped Minecraft runtime jars in the ../libs directory.
These placeholder files exist so the Gradle task `prepareLabyRuntime` has a source to copy
from when bootstrapping a development workspace without the real dependencies.
